pipeline{
    agent any

    tools {
        jdk 'java-17'
        maven 'maven'
    }

    stages{
        stage('gitcheckout'){
            steps{
                git url : 'https://github.com/Prajna2363/test-1.git' ,  branch : 'main'

            }
        }
        stage('Compile'){
            steps{
                sh " mavn compile "
            }
        }

    }
}